:root{
  --bg: #070b12;
  --panel: rgba(255,255,255,0.06);
  --panel2: rgba(255,255,255,0.08);
  --text: rgba(255,255,255,0.92);
  --muted: rgba(255,255,255,0.66);
  --muted2: rgba(255,255,255,0.52);
  --line: rgba(255,255,255,0.10);
  --accent: #a9e6ff;
  --accent2:#d6c8ff;
  --shadow: 0 18px 50px rgba(0,0,0,0.55);
  --radius: 18px;
  --radius2: 26px;
  --max: 1120px;
}

*{ box-sizing: border-box; }
html,body{ height:100%; }
body{
  margin:0;
  font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  color: var(--text);
  background: radial-gradient(1200px 600px at 20% 0%, rgba(169,230,255,0.12), transparent 60%),
              radial-gradient(900px 500px at 85% 20%, rgba(214,200,255,0.10), transparent 55%),
              var(--bg);
  overflow-x:hidden;
}

img{ max-width:100%; display:block; }
a{ color: inherit; text-decoration: none; }
.container{ width:min(var(--max), calc(100% - 40px)); margin:0 auto; }

.topbar{
  position: sticky;
  top:0;
  z-index:100;
  backdrop-filter: blur(10px);
  background: rgba(7,11,18,0.60);
  border-bottom: 1px solid var(--line);
}
.topbar__inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:14px 0;
}

.brand{
  display:flex;
  gap:10px;
  align-items:baseline;
  letter-spacing: .2px;
}
.brand__mark{
  font-family: "Playfair Display", serif;
  font-size: 18px;
  opacity:.95;
}
.brand__name{
  font-weight:600;
  font-size:16px;
}

.nav{ position:relative; }
.nav__menu{
  display:flex;
  gap:16px;
  align-items:center;
}
.nav__menu a{
  padding:10px 10px;
  border-radius: 12px;
  color: var(--muted);
}
.nav__menu a:hover{ background: rgba(255,255,255,0.06); color: var(--text); }

.nav__cta{
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.06);
  color: var(--text) !important;
}

.nav__toggle{
  display:none;
  width:44px; height:44px;
  border-radius: 14px;
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.06);
  align-items:center;
  justify-content:center;
  gap:5px;
}
.nav__toggle span{
  display:block;
  width:18px; height:2px;
  background: rgba(255,255,255,0.78);
  border-radius: 10px;
}

.hero{
  position:relative;
  padding: 56px 0 18px;
}
.hero__bg{
  position:absolute;
  inset:-80px -40px auto -40px;
  height: 520px;
  background: radial-gradient(800px 420px at 30% 35%, rgba(169,230,255,0.10), transparent 60%),
              radial-gradient(800px 420px at 80% 35%, rgba(214,200,255,0.10), transparent 60%);
  pointer-events:none;
  filter: blur(0px);
}
.hero__grid{
  display:grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 34px;
  align-items:center;
}
.eyebrow{
  margin:0 0 10px;
  color: var(--muted);
  letter-spacing:.18em;
  text-transform: uppercase;
  font-size: 12px;
}
.hero__title{
  font-family: "Playfair Display", serif;
  font-size: clamp(32px, 4vw, 52px);
  line-height: 1.02;
  margin:0 0 14px;
}
.accent{
  background: linear-gradient(90deg, var(--accent), var(--accent2));
  -webkit-background-clip:text;
  background-clip:text;
  color: transparent;
}
.hero__subtitle{
  margin: 0 0 22px;
  color: var(--muted);
  line-height:1.55;
  font-size: 16px;
}

.hero__actions{ display:flex; gap:12px; flex-wrap:wrap; margin-bottom: 22px; }

.btn{
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.06);
  color: var(--text);
  padding: 12px 16px;
  border-radius: 14px;
  font-weight:600;
  cursor:pointer;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:10px;
  transition: transform .18s ease, background .18s ease, border-color .18s ease;
}
.btn:hover{ transform: translateY(-1px); background: rgba(255,255,255,0.09); }
.btn--primary{
  border-color: rgba(169,230,255,0.35);
  background: linear-gradient(90deg, rgba(169,230,255,0.16), rgba(214,200,255,0.12));
}
.btn--ghost{ background: transparent; }
.btn--disabled{
  opacity:.55; cursor:not-allowed;
}
.btn--disabled:hover{ transform:none; background: rgba(255,255,255,0.06); }

.hero__badges{
  display:flex;
  gap:12px;
  flex-wrap:wrap;
}
.badge{
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.04);
  padding: 10px 12px;
  border-radius: 14px;
  min-width: 160px;
}
.badge__top{ display:block; color: var(--muted2); font-size:12px; }
.badge__bot{ display:block; color: var(--text); font-weight:600; font-size:14px; margin-top:3px; }

.hero__media{
  position:relative;
}
.photo{
  border-radius: var(--radius2);
  overflow:hidden;
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(255,255,255,0.03);
  box-shadow: var(--shadow);
}
.photo--hero img{
  width:100%;
  height: 440px;
  object-fit: cover;
}
.photo__glow{
  position:absolute;
  inset: -10px;
  pointer-events:none;
  background: radial-gradient(260px 160px at 65% 20%, rgba(169,230,255,0.16), transparent 65%),
              radial-gradient(240px 150px at 25% 55%, rgba(214,200,255,0.14), transparent 70%);
  filter: blur(10px);
}

.hero__card{
  position:absolute;
  left: -18px;
  bottom: -14px;
  width: min(360px, 92%);
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(7,11,18,0.72);
  backdrop-filter: blur(10px);
  border-radius: 18px;
  padding: 14px 14px;
}
.hero__cardTitle{
  margin:0;
  font-weight:700;
}
.hero__cardText{
  margin:6px 0 0;
  color: var(--muted);
  line-height:1.45;
  font-size: 14px;
}

.section{
  padding: 72px 0;
}
.section--alt{
  background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
}
.section__grid{
  display:grid;
  grid-template-columns: 0.9fr 1.1fr;
  gap: 34px;
  align-items:center;
}
.section__title{
  font-family: "Playfair Display", serif;
  font-size: 34px;
  margin: 0 0 10px;
}
.section__lead{
  margin: 0;
  color: var(--muted);
  line-height:1.6;
}
.section__head{
  max-width: 760px;
  margin-bottom: 28px;
}

.photo--about img{
  height: 460px;
  object-fit: cover;
}

.cards{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
  margin-top: 22px;
}
.card{
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.04);
  border-radius: var(--radius);
  padding: 16px;
}
.card h3{ margin: 0 0 8px; }
.card p{ margin: 0; color: var(--muted); line-height:1.55; }

.note{
  margin-top: 18px;
  border-left: 3px solid rgba(169,230,255,0.55);
  padding: 12px 14px;
  background: rgba(255,255,255,0.03);
  border-radius: 14px;
}
.note p{ margin:0; color: var(--muted); line-height:1.55; }

.grid3{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14px;
}
.tile{
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.04);
  border-radius: var(--radius);
  padding: 18px;
}
.tile h3{ margin:0 0 8px; }
.tile p{ margin:0; color: var(--muted); line-height:1.55; }

.services{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
}
.service{
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.04);
  border-radius: var(--radius);
  padding: 18px;
}
.service h3{ margin:0 0 8px; }
.service p{ margin:0; color: var(--muted); line-height:1.55; }

.ctaBox{
  margin-top: 18px;
  border: 1px solid rgba(169,230,255,0.22);
  background: linear-gradient(90deg, rgba(169,230,255,0.10), rgba(214,200,255,0.08));
  border-radius: var(--radius2);
  padding: 18px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 14px;
}
.ctaBox h3{ margin:0 0 6px; }
.ctaBox p{ margin:0; color: var(--muted); }

.gallery{
  display:grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 12px;
}
.gallery__item{
  border: 1px solid rgba(255,255,255,0.10);
  border-radius: var(--radius);
  overflow:hidden;
  background: rgba(255,255,255,0.03);
}
.gallery__item img{
  width:100%;
  height: 240px;
  object-fit: cover;
}
.gallery__item:nth-child(1){ grid-column: span 7; }
.gallery__item:nth-child(2){ grid-column: span 5; }
.gallery__item:nth-child(3){ grid-column: span 5; }
.gallery__item:nth-child(4){ grid-column: span 7; }

.contact{
  display:grid;
  grid-template-columns: 1.05fr 0.95fr;
  gap: 16px;
  align-items:start;
}
.contact__cards{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
  margin-top: 18px;
}
.mini{
  border: 1px solid var(--line);
  background: rgba(255,255,255,0.04);
  border-radius: var(--radius);
  padding: 16px;
}
.mini h3{ margin:0 0 6px; }
.mini p{ margin:0; color: var(--muted); line-height:1.55; }
.link{ display:inline-block; margin-top: 10px; color: rgba(169,230,255,0.95); }

.contact__panel{
  border: 1px solid rgba(255,255,255,0.10);
  background: rgba(255,255,255,0.04);
  border-radius: var(--radius2);
  padding: 18px;
}
.contact__panel h3{ margin: 0 0 8px; }
.muted{ color: var(--muted); }
.small{ font-size: 12px; color: var(--muted2); }

form{ margin-top: 12px; }
label{
  display:block;
  margin-bottom: 12px;
  font-weight: 600;
  color: rgba(255,255,255,0.86);
}
input, select, textarea{
  margin-top: 8px;
  width:100%;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,0.14);
  background: rgba(7,11,18,0.60);
  color: var(--text);
  padding: 12px 12px;
  outline:none;
}
textarea{ resize: vertical; }
input::placeholder, textarea::placeholder{ color: rgba(255,255,255,0.42); }

.form__actions{ display:flex; gap: 10px; flex-wrap:wrap; margin-top: 10px; }
.form__output{ margin-top: 14px; }
.form__output textarea{ height: 120px; }

.footer{
  border-top: 1px solid var(--line);
  padding: 26px 0;
  background: rgba(255,255,255,0.02);
}
.footer__inner{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap: 12px;
}
.footer__brand{ margin:0; font-weight: 700; }
.footer__muted{ margin:6px 0 0; color: var(--muted); }
.footer__links{ display:flex; gap: 12px; flex-wrap:wrap; }
.footer__links a{ color: var(--muted); padding: 8px 10px; border-radius: 12px; }
.footer__links a:hover{ background: rgba(255,255,255,0.06); color: var(--text); }

.waFloat{
  position: fixed;
  right: 18px;
  bottom: 18px;
  width: 54px;
  height: 54px;
  border-radius: 18px;
  border: 1px solid rgba(255,255,255,0.14);
  background: rgba(255,255,255,0.06);
  color: rgba(255,255,255,0.85);
  font-weight: 800;
  letter-spacing: .04em;
  cursor: not-allowed;
  opacity: .70;
}
.toTop{
  position: fixed;
  left: 18px;
  bottom: 18px;
  width: 46px;
  height: 46px;
  display:grid;
  place-items:center;
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,0.14);
  background: rgba(255,255,255,0.06);
  color: rgba(255,255,255,0.85);
}

.reveal{
  opacity: 0;
  transform: translateY(10px);
  transition: opacity .6s ease, transform .6s ease;
}
.reveal.is-visible{
  opacity: 1;
  transform: translateY(0);
}

/* Responsivo */
@media (max-width: 980px){
  .hero__grid{ grid-template-columns: 1fr; }
  .hero__card{ left: 10px; }
  .section__grid{ grid-template-columns: 1fr; }
  .contact{ grid-template-columns: 1fr; }
  .cards{ grid-template-columns: 1fr; }
  .services{ grid-template-columns: 1fr; }
  .grid3{ grid-template-columns: 1fr; }
  .gallery__item:nth-child(1),
  .gallery__item:nth-child(2),
  .gallery__item:nth-child(3),
  .gallery__item:nth-child(4){ grid-column: span 12; }
}

@media (max-width: 760px){
  .nav__toggle{ display:inline-flex; }
  .nav__menu{
    position:absolute;
    right:0;
    top:54px;
    width:min(320px, calc(100vw - 40px));
    display:none;
    flex-direction:column;
    align-items:stretch;
    padding: 10px;
    border-radius: 18px;
    border: 1px solid rgba(255,255,255,0.12);
    background: rgba(7,11,18,0.82);
    backdrop-filter: blur(10px);
  }
  .nav__menu.is-open{ display:flex; }
  .nav__menu a{ padding: 12px 12px; }
}
